# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.workers.max=2
org.gradle.parallel=false


# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use legacy packaging for native libraries to fix 16KB alignment issues
# android.jniLibs.useLegacyPackaging=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# Limit architectures to reduce bundle size and fix alignment issues
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable build cache for faster builds
org.gradle.caching=true

# Configure Kotlin (if used)
kotlin.code.style=official

# Expo specific configurations
# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
# This is critical for fixing 16KB alignment issues
# expo.useLegacyPackaging=true

# JavaScript engine
expo.jsEngine=hermes

# Enable new native pages configuration
android.useNewNativePagesConfiguration=true

# 重要修正：啟用 Jetifier 來支援舊版庫
android.enableJetifier=true
android.nativePageSize=16384

# Build performance optimizations
org.gradle.daemon=true
org.gradle.configureondemand=true

# 添加這些缺失的重要配置
android.native.buildOutput=verbose

# 修正版本配置（移除有問題的配置）
# android.targetSdkVersion=35
# android.compileSdkVersion=35

# 添加 React Native 配置
react.internal.disableJavaVersionCheck=true

MYAPP_UPLOAD_STORE_FILE= @katiehuang__audio-recorder-mvp.jks
MYAPP_UPLOAD_KEY_ALIAS=b6be1d7ec4a7e70668741dc1a942d999
MYAPP_UPLOAD_STORE_PASSWORD=c4be68e7cef12dbddbcf3bc0cacc46bc
MYAPP_UPLOAD_KEY_PASSWORD=fd6939f3ffd6868487c4fdb532c21f78